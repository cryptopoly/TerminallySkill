{
  "id": "uv-run",
  "name": "uv run",
  "executable": "uv",
  "subcommands": ["run"],
  "description": "Run a command inside a uv-managed Python environment",
  "category": "python",
  "tags": ["python", "uv", "virtualenv", "run"],
  "dangerLevel": "safe",
  "docsUrl": "https://docs.astral.sh/uv/reference/cli/",
  "options": [
    {
      "id": "python",
      "long": "--python",
      "label": "Python version",
      "description": "Use a specific Python interpreter or version",
      "type": "string"
    },
    {
      "id": "with",
      "long": "--with",
      "label": "Extra dependency",
      "description": "Install an extra dependency before running the command",
      "type": "repeatable"
    }
  ],
  "positionalArgs": [
    {
      "id": "command",
      "label": "Command",
      "description": "Command to run in the uv environment",
      "type": "string",
      "required": true,
      "position": 0
    },
    {
      "id": "args",
      "label": "Arguments",
      "description": "Additional arguments passed to the command",
      "type": "string",
      "variadic": true,
      "position": 1
    }
  ],
  "examples": [
    {
      "label": "Run pytest in the managed environment",
      "values": {
        "command": "pytest"
      }
    },
    {
      "label": "Run a module with a specific Python version",
      "values": {
        "python": "3.12",
        "command": "python",
        "args": ["-m", "app"]
      }
    }
  ]
}
